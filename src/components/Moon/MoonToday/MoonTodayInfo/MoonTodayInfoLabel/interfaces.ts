import { IColors } from "../../../../../providers/ThemeProvider/interfaces";

export interface IMoonTodayInfoLabel {
  offSetY: number;
  label: string;
  theme: IColors;
  value: any;
  x: number;
  y: number;
}
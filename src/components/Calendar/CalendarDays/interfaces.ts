import { IColors } from "../../../providers/ThemeProvider/interfaces";

export interface ICalendarDays {
  angle: number;
  day: number;
  radius: number;
  theme: IColors;
  x: number;
  y: number;
}